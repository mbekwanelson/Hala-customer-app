import 'package:flutter/material.dart';

class TempHome extends StatelessWidget {
  const TempHome();

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Center(
        child: Text("Temp Home Page"),
      ),
    );
  }
}
